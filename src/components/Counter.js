// import React from 'react';
// import { useSelector, useDispatch } from 'react-redux';
// import { decrement, deleteAll, increment, multiply } from '../Redux/counter/counter';

// function CounterPage() {
//   const count = useSelector((state) => state.counter.value);
//   const dispatch = useDispatch();

//   return (
//     <div className="text-center mt-5">
//       <h1>Counter Redux Component</h1>
//       <h2>{count}</h2>
//       <button onClick={() => dispatch(increment())}>Increment</button><br/>
//       <button disabled={count === 0} onClick={() => dispatch(decrement())}>Decrement</button><br/>
//       <button onClick={() => dispatch(multiply())}>Multiply</button><br/>
//       <button onClick={() => dispatch(deleteAll())}>Delete All</button>
//     </div>
//   );
// }

// export default CounterPage;
